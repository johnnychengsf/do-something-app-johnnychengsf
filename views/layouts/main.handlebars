<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Do Something</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <nav>
        <div class="Title">
            <img
                id="Logo"
                src="/Images/Do Something-1.png"
                alt="Pic. Logo"
                style="float:left;width:17%;height:130px;"
            />
            <h1>
                Do Something
                <h3 style="text-align: center;">Your daily Task Manager</h3></h1>
        </div>

        {{#if logged_in}}
            <a href="/login" id="logout">Logout</a>
        {{else}}
            <a href="/login">Login</a>
        {{/if}}
    </nav>
    <body>
        {{{body}}}
        {{#if logged_in}}
            <script src="/js/logout.js"></script>
        {{/if}}
    </body>
</html>